id=${moduleId}
name=${api} - DAndroid
version=${versionName} (${versionCode})
versionCode=${versionCode}
author=${authorList}
description=Another enhanced implementation of DAndroid Framework. Supports Android 8.1 ~ 14. ${requirement}.
updateJson=${updateJson}
